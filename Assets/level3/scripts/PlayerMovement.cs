using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour

{
    /*
    private FirstPersonInputController controls;

    private Vector3 velocity;

    private Vector2 move;

    private CharacterController characterController;

    public bool isGrounded;

    private float moveSpeed = 10f;

    private float gravity = 9.81f;

    public Transform Ground;

    private float JumpHeight = 0.4f;

    public LayerMask grownMask;

    public float distanceToGround = 0.4f;
    // Start is called before the first frame update

    private void Awake()
    {
        controls = new FirstPersonInputController;
        characterController = GetComponent<CharacterController>();
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void Gravity()
    {
        isGrounded = Physics.CheckSphere[Ground.position, distanceToGround, grownMask];

        if (isGrounded)
        {

        }
    }
    */
}
